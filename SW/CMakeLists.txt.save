cmake_minimum_required (VERSION 3.7)
project (RHA)

SET(RHA_LIBS
	${CMAKE_SOURCE_DIR}/lib/
chuck_handler     joint_handler  readme.txt  servo_rha
cytron_g15_servo  joint_rha      rha_types   utilities
debug      piduino        robot_rha


# Include directories where code is:
include_directories(${RHA_LIBS})

# Add the executable
add_executable(${PROJEC_NAME} src/main.cpp)

